# 线程交接总结：柜体设计 + React Flow 拓扑图服务器部署

日期：2026-06-24

## 1. 本轮结论

本轮已把本地更新后的 `cabinet-design` 项目部署到服务器，没有更新 GitHub。

生产访问地址：

```text
http://192.168.100.127:3188/
http://192.168.100.127:3188/topology/19
```

服务器部署目录：

```text
/home/cabinet-design/releases/20260624202135
/home/cabinet-design/current -> /home/cabinet-design/releases/20260624202135
```

容器信息：

```text
container_name: cabinet-design
image: cabinet-design:latest
port: 3188:3000
compose project: 20260624202135
```

本轮服务器上新增了本地环境变量文件：

```text
/home/cabinet-design/releases/20260624202135/.env.production
```

同时新增了不含敏感信息的 Compose 项目名文件：

```text
/home/cabinet-design/releases/20260624202135/.env
COMPOSE_PROJECT_NAME=20260624202135
```

作用：从 `/home/cabinet-design/current` 这个符号链接目录执行 `docker compose ps` 时，也能正确识别本次 release 对应的容器。

`.env.production` 只保存在服务器，不要提交到 GitHub，不要写入公开文档。里面用于配置报价系统 API 访问：

```text
QUOTE_SYSTEM_BASE_URL
QUOTE_SYSTEM_USERNAME
QUOTE_SYSTEM_PASSWORD
```

## 2. 已完成能力

已实现并部署：

- 报价单转 `Topology JSON`
- React Flow 拓扑图页面：`/topology/[quoteId]`
- ELK 自动布局
- 默认逻辑连线
- 真实链路优先展示
- 按系统视图筛选：系统拓扑图、网络系统、智能控制、影音系统、未分类设备等
- 节点搜索、选中、高亮
- 拓扑图和机柜图共用设备 ID
- 点击拓扑节点高亮机柜设备
- 点击机柜设备高亮拓扑节点
- 未识别设备单独列出并提示人工确认
- 拖拽节点后保存布局到浏览器本地存储
- 导出 PNG / SVG / JSON / PDF
- 批量导出交付资料清单
- 不影响原有机柜布局功能

## 3. 本轮部署操作摘要

本地打包时排除了缓存和构建产物：

```text
node_modules
.next
.npm-cache
.git
output
test-results
.tmp-test
.playwright-cli
*.log
tsconfig.tsbuildinfo
```

上传到服务器的源码包：

```text
/tmp/cabinet-design-20260624202135.tar.gz
```

服务器构建使用：

```bash
cd /home/cabinet-design/releases/20260624202135
docker compose config
docker compose build
docker rm -f cabinet-design
docker compose up -d
```

## 4. 已验证结果

本地开发环境验证：

```bash
npm run test:topology
npm run test:network-layout
npm run typecheck
npm run lint
npm run build
```

全部通过。

服务器验证：

```text
docker compose build: 成功
container health: healthy
http://127.0.0.1:3188/                              -> 200
http://127.0.0.1:3188/topology/19                   -> 200
http://127.0.0.1:3188/api/topology/quote-system/19  -> 200
```

从本机访问服务器也已验证：

```text
http://192.168.100.127:3188/                              -> 200
http://192.168.100.127:3188/topology/19                   -> 200
http://192.168.100.127:3188/api/topology/quote-system/19  -> 200
```

报价系统 API 中间层返回了报价单：

```text
quoteId: QT20260624-0019
projectName: 小夏报价单
```

敏感信息检查：

- 未发现明文密码或 token 写入项目文件。
- 只存在环境变量名 `QUOTE_SYSTEM_PASSWORD`。
- 服务器真实凭据只保存在 `.env.production`，不要复制到聊天记录、README、截图或 GitHub。

## 5. 关键文件入口

拓扑页面：

```text
app/topology/[quoteId]/page.tsx
```

报价系统 API 中间层：

```text
app/api/topology/quote-system/[quoteId]/route.ts
lib/topology/quoteSystemApi.ts
```

拓扑生成核心：

```text
lib/topology/buildTopology.ts
lib/topology/classifyDevice.ts
lib/topology/autoLinkDevices.ts
lib/topology/layoutWithElk.ts
lib/topology/topologyViews.ts
```

导出相关：

```text
lib/topology/exportTopology.ts
lib/topology/exportNaming.ts
lib/topology/deliveryManifest.ts
components/topology/TopologyToolbar.tsx
```

机柜联动：

```text
lib/topology/buildCabinetProject.ts
components/topology/TopologyCabinetPreview.tsx
components/topology/TopologyCanvas.tsx
```

测试脚本：

```text
scripts/测试拓扑生成.ts
scripts/测试网络机柜布局.ts
tsconfig.test.json
```

## 6. 常用维护命令

查看当前生产容器：

```bash
cd /home/cabinet-design/current
docker compose ps
docker logs cabinet-design --tail 100
```

重启服务：

```bash
cd /home/cabinet-design/current
docker compose restart
```

重新构建并替换容器：

```bash
cd /home/cabinet-design/current
docker compose build
docker rm -f cabinet-design
docker compose up -d
```

回滚到上一版源码目录：

```bash
cd /home/cabinet-design/releases/20260621163655
docker compose build
docker rm -f cabinet-design
docker compose up -d
ln -sfn /home/cabinet-design/releases/20260621163655 /home/cabinet-design/current
```

## 7. 后续建议

下一步优先做这些：

1. 把人工修正后的连线、端口、线缆类型保存到后端，而不是只存在浏览器本地。
2. 增加“报价系统一键进入拓扑图”的入口按钮。
3. 给真实端口关系做表格化编辑：源设备、源端口、目标设备、目标端口、线缆类型、备注。
4. 导出 PDF 时增加封面、项目名称、报价编号、生成时间和施工说明。
5. 增加项目级模板：别墅、展厅、影音室、KTV、酒店客房、办公室。
6. 安全上建议收紧 SSH 登录：更换强密码、限制来源 IP、启用密钥登录、关闭公网弱口令入口。

## 8. 给下一个大模型的接续提示词

```text
你现在接手一个 Next.js / React / TypeScript 项目：

本地路径：
C:\Users\zhang\uoloproject\cabinet-design

线上地址：
http://192.168.100.127:3188/
http://192.168.100.127:3188/topology/19

服务器当前 release：
/home/cabinet-design/releases/20260624202135
/home/cabinet-design/current

注意：
1. 不要把服务器密码、报价系统密码、.env.production 写入 GitHub、README、截图或聊天记录。
2. 当前部署没有更新 GitHub，后续也先按本地源码 + 服务器 Docker 部署推进。
3. 修改后必须跑：
   npm run test:topology
   npm run test:network-layout
   npm run typecheck
   npm run lint
   npm run build
4. 部署端口固定为 3188:3000。
5. 重点继续开发：人工修正连线保存、端口级连接关系、报价系统入口按钮、PDF 交付资料优化。

请先阅读：
- app/topology/[quoteId]/page.tsx
- components/topology/
- lib/topology/
- types/topology.ts
- scripts/测试拓扑生成.ts
- scripts/测试网络机柜布局.ts
- 本总结文档

然后继续实现“人工修正拓扑关系并保存到后端/本地项目文件”的功能，要求保持拓扑图和机柜图使用同一套 deviceId，并保持现有导出功能可用。
```
